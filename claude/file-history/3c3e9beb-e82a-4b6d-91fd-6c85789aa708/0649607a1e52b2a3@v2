#![allow(dead_code)]
struct Solution;

impl Solution {
    pub fn four_sum(nums: Vec<i32>, target: i32) -> Vec<Vec<i32>> {
        let mut result: Vec<Vec<i32>> = Vec::new();
        let len = nums.len();
        let mut nums = nums;
        nums.sort_unstable();

        for i in 0..len {
            if i > 0 && nums[i] == nums[i - 1] {
                continue;
            }

            for j in (i + 1)..(len - 2) {
                if j > (i + 1) && nums[j] == nums[j - 1] {
                    continue;
                }

                let mut left = j + 1;
                let mut right = len - 1;

                while left < right {
                    let sum = nums[i] + nums[j] + nums[left] + nums[right];

                    if sum == target {
                        result.push(vec![nums[i], nums[j], nums[left], nums[right]]);
                    } else if sum < target {
                        left += 1;
                        while left < right && nums[left] == nums[left - 1] {
                            left += 1;
                        }
                    } else if sum > target {
                        right -= 1;
                        while left < right && nums[right] == nums[right + 1] {
                            right -= 1;
                        }
                    }
                }
            }
        }

        result
    }
}

#[cfg(test)]
pub mod tests {
    use super::*;

    #[test]
    fn test_one() {
        let input = vec![1, 0, -1, 0, -2, 2];
        let expected = vec![vec![-2, -1, 0, 2], vec![-2, 0, 0, 2], vec![-1, 0, 0, 1]];
        let answer = Solution::four_sum(input, 0);
        assert_eq!(expected, answer);
    }

    #[test]
    fn test_four_zeroes() {
        let input: Vec<i32> = vec![0, 0, 0, 0];
        let expected = vec![vec![0, 0, 0, 0]];
        let answer = Solution::four_sum(input, 0);
        assert_eq!(expected, answer);
    }
}
